/**
 * Default Warp Registry base URL, used when neither the --registry CLI flag
 * nor the WARP_REGISTRY_URL environment variable is provided.
 * @type {string}
 */
export const DEFAULT_REGISTRY_URL = "https://registry.example.invalid";
